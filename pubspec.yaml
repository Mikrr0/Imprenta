name: proyecto
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
 
  provider: ^6.1.5+1
  google_fonts: ^8.1.0
  rut_validator:
    path: ./rut_validator
  # Al dejarlos vacíos, Flutter descargará la última versión compatible automáticamente
  firebase_core: 
  firebase_auth: 
  cloud_firestore: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true